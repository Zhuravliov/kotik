#ifndef __STACK2_H__
#define __STACK2_H__

#include<stdio.h>
#include <stdbool.h>

void show(void);
int push(int n);
int pop(void);
bool is_empty(void);
bool is_full(void);

#endif // __STACK2_H__
